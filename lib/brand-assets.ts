export function brandAsset(centerSlug: string, filename: string): string {
  return `/brands/${centerSlug}/${filename}`;
}

export const FOREST_DEFAULT_HERO_IMAGE = "/brands/forest-lighthouse/yoga-lines.png";
